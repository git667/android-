package com.example.myapplication;

import org.junit.Test;

import static org.junit.Assert.*;

/**
 * Example local地方的 unit单元 test, which will execute执行 on the development发展 machine 机器(host.主办)
 *
 * @see <a href="http://d.android.com/tools/testing">Testing documentation</a>
 */
public class ExampleUnitTest {
    @Test
    public void addition_isCorrect() { //addition附加 Correct正确
        assertEquals(4, 2 + 2);//这些有大小写组合的都可以上百度搜例如：assertEquals
    }
}
